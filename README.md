# Memed Prettier config

> [Prettier](https://prettier.io) configuration for Memed projects.

## Usage

**Install**:

```bash
$ yarn add --dev @memed-dev/prettier-config
```

**Edit your `package.json`**:

```jsonc
{
  // ...
  "prettier": "@memed-dev/prettier-config"
}
```
